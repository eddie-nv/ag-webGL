'use client'

/**
 * Placeholder for the v2 stage / active-agent overlay. The current pipeline
 * runs synchronously and emits CustomEvents; once we surface agent-state
 * snapshots through the SSE stream, this component will read currentStage /
 * activeAgentName from a shared context.
 */
export function StageIndicator() {
  return null
}
